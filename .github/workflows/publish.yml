name: Publish to npm

on:
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (patch, minor, major)'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Setup Java (for Wasm build)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Setup Maven
        uses: stCarolas/setup-maven@v4
        with:
          maven-version: '3.9.5'

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync with remote (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "🔄 Fetching latest changes from remote..."
          git fetch origin main
          git checkout main
          git pull origin main --rebase || git pull origin main

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Bump version (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: |
          LOCAL_VERSION=$(node -p "require('./package.json').version")
          echo "📦 Local package.json version: $LOCAL_VERSION"

          NPM_VERSION=$(npm view node-plantuml-2 version 2>/dev/null || echo "")

          if [ -z "$NPM_VERSION" ]; then
            echo "📦 No existing version on npm, starting fresh"
            BASE_VERSION=$LOCAL_VERSION
          else
            echo "📦 Latest npm version: $NPM_VERSION"

            COMPARE=$(node -e "
              const local = '$LOCAL_VERSION'.split('.').map(Number);
              const npm = '$NPM_VERSION'.split('.').map(Number);
              for (let i = 0; i < 3; i++) {
                if (local[i] > npm[i]) {
                  console.log('local');
                  process.exit(0);
                } else if (local[i] < npm[i]) {
                  console.log('npm');
                  process.exit(0);
                }
              }
              console.log('equal');
            ")

            if [ "$COMPARE" = "npm" ] || [ "$COMPARE" = "equal" ]; then
              echo "⚠️  npm version ($NPM_VERSION) is newer or equal to local ($LOCAL_VERSION)"
              npm version $NPM_VERSION --no-git-tag-version --allow-same-version
              BASE_VERSION=$NPM_VERSION
            else
              echo "✅ Local version ($LOCAL_VERSION) is newer than npm ($NPM_VERSION)"
              BASE_VERSION=$LOCAL_VERSION
            fi
          fi

          CURRENT=$(node -p "require('./package.json').version")
          if [ "$CURRENT" != "$BASE_VERSION" ]; then
            npm version $BASE_VERSION --no-git-tag-version --allow-same-version
          fi

          echo "🆕 Incrementing version (${{ github.event.inputs.version }})..."
          npm version ${{ github.event.inputs.version }} --no-git-tag-version
          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "🆕 New version: $NEW_VERSION"

          if npm view node-plantuml-2@$NEW_VERSION version > /dev/null 2>&1; then
            echo "⚠️  Version $NEW_VERSION already exists, incrementing again..."
            npm version ${{ github.event.inputs.version }} --no-git-tag-version
            NEW_VERSION=$(node -p "require('./package.json').version")

            if npm view node-plantuml-2@$NEW_VERSION version > /dev/null 2>&1; then
              echo "❌ Error: Version $NEW_VERSION also exists on npm"
              exit 1
            fi
          fi

          echo "VERSION=$NEW_VERSION" >> $GITHUB_ENV

      - name: Bump version (release)
        if: github.event_name == 'release'
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Release version: $VERSION"

          if npm view node-plantuml-2@$VERSION version > /dev/null 2>&1; then
            echo "⚠️  Warning: Version $VERSION already exists on npm"
          fi

          npm version $VERSION --no-git-tag-version --allow-same-version
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Verify version before publish
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "📦 Preparing to publish version: $VERSION"

          if npm view node-plantuml-2@$VERSION version > /dev/null 2>&1; then
            echo "❌ Error: Version $VERSION already exists on npm registry"
            exit 1
          fi

          echo "✅ Version $VERSION is ready to publish"

      - name: Download PlantUML JAR
        run: |
          echo "📥 Downloading latest PlantUML JAR..."
          npm run build:all:jar-only || echo "JAR download failed, but continuing..."

      - name: Build Wasm module (optional)
        continue-on-error: true
        run: |
          echo "🔨 Building Wasm module (this may take several minutes)..."
          echo "⚠️  Note: Wasm build is experimental and may fail"
          npm run build:wasm:bytecoder || echo "Wasm build failed, will use Java executor as fallback"

      - name: Build
        run: npm run build || echo "No build script found, skipping"

      - name: Verify NPM authentication
        run: |
          echo "🔍 Verifying npm authentication..."
          npm whoami || {
            echo "❌ Error: Not authenticated to npm"
            echo "💡 Please ensure NPM_TOKEN secret is configured in GitHub repository secrets"
            exit 1
          }
          echo "✅ Successfully authenticated to npm"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish to npm
        run: |
          echo "📦 Publishing to npm..."
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        continue-on-error: false

      - name: Create Git tag and push (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: |
          git add package.json package-lock.json

          if git diff --staged --quiet; then
            echo "⚠️  No changes to commit"
          else
            git commit -m "chore: bump version to ${{ env.VERSION }}"
          fi

          echo "🔄 Pulling latest changes from remote..."
          git fetch origin main
          git pull origin main --rebase || git pull origin main --no-edit || {
            echo "❌ Failed to sync with remote"
            exit 1
          }

          if git rev-parse "v${{ env.VERSION }}" >/dev/null 2>&1; then
            echo "⚠️  Tag v${{ env.VERSION }} already exists locally"
          else
            git tag -a "v${{ env.VERSION }}" -m "Release v${{ env.VERSION }}"
          fi

          echo "📤 Pushing changes to remote..."
          git push origin HEAD:main || {
            echo "❌ Failed to push to main branch"
            exit 1
          }

          if git ls-remote --tags origin | grep -q "refs/tags/v${{ env.VERSION }}$"; then
            echo "ℹ️  Tag v${{ env.VERSION }} already exists on remote"
          else
            git push origin "v${{ env.VERSION }}" || {
              echo "⚠️  Failed to push tag, but code was pushed successfully"
            }
          fi

          echo "✅ Successfully pushed version ${{ env.VERSION }}"

      - name: Create GitHub Release (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ env.VERSION }}
          name: Release v${{ env.VERSION }}
          body: |
            ## Changes in v${{ env.VERSION }}

            See [CHANGELOG.md](CHANGELOG.md) for details.

            ### ✨ Features
            - Pure Node.js support (no Java required)
            - Multiple output formats (PNG, SVG, EPS, ASCII, Unicode)
            - Full UTF-8 and Chinese character support
          draft: false
          prerelease: false

